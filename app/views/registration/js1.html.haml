= render 'layouts/navigation'

= render 'layouts/aside'

%div#main-content

  %h1 JS

  %p.task Fix the bugs in the inline JavaScript code without removing the setTimeout function.

  :javascript
    $(function() {
      $("#registration-form").on("submit", function(event) {
        setTimeout(function() {
          this.submit();
        }, 50);
        event.preventDefault();
      });
    });

  = render 'form'
